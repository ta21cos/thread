# Frontend Environment Variables - Test Mode
# Used by Playwright E2E tests with Vite --mode test

# Backend server port for proxy configuration
VITE_BACKEND_API_ENDPOINT=http://localhost:3000
VITE_BACKEND_WS_ENDPOINT=ws://localhost:3000

# NOTE: E2E test mode - bypasses Clerk authentication
VITE_E2E_TEST=true

# NOTE: Dummy key to prevent startup error (not used in test mode)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_e2e_dummy_key
